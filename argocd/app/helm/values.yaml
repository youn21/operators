grr:
  mariadb:
    enabled: false
    externalHost: mariadb
    auth:
      existingSecret: mariadb-password
  ingress:
    enabled: true
    className: "openshift-default"
    annotations:
      route.openshift.io/termination: edge
    hosts:
      - host: change_me.apps.anf.math.cnrs.fr
        paths:
          - path: /
            pathType: Prefix
